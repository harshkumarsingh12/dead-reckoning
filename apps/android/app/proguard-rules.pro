# Minification is off for now (see build.gradle.kts). Turn it on only after a
# verification pass on a real device — an obfuscated sensor pipeline that silently
# drops samples is not a debugging session anyone wants during a hackathon.
